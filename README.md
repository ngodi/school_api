# School Management API

## Available Endpoints

- `GET /health` — Returns `{ status: 'ok' }` if the API is running.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the server:
   ```bash
   npm start
   ```
